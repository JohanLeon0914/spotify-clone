---
import Layout from "@/layouts/Layout.astro";
import PlayListItemCard from "@/components/PlayListItemCard.astro";
import Greating from '@/components/Greating.svelte';
import { fetchPlaylists } from "@/lib/data";

const playlists = await fetchPlaylists();
---

<Layout title="Spotify clone">
  <div
    id="playlist-container"
    class="relative transition-all duration-1000 bg-green-600"
  >
    <!-- <PageHeader /> -->

    <div class="relative z-10 px-6 pt-10">
      <Greating />
      <div class="flex flex-wrap mt-6 gap-4 pb-5">
        {playlists.map((playlist) => (
          <PlayListItemCard playlist={playlist} />
        ))}
      </div>

      <div
        class="absolute inset-0 bg-gradient-to-t from-zinc-900 via-zinc-900/80 -z-[1]"
      />

      <a 
  href="/createPlaylist" 
  class="mb-4 inline-block mt-5 px-4 py-2 bg-green-600 text-white font-semibold rounded-full hover:bg-green-700 transition duration-200 ease-in-out shadow-md focus:outline-none focus:ring-2 focus:ring-green-500"
>
  + Crear playlist
</a>
    </div>

    <style></style>
  </div>
</Layout>

